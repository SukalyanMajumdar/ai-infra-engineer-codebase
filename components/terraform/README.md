# Terraform Layer

The infrastructure source of truth is the separate `ai-infra-engineer` repository.

The portfolio repository documents its architecture and lifecycle but intentionally does not duplicate environment-specific Terraform state/backend configuration.
