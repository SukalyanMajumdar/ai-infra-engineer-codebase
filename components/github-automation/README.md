# GitHub Automation

The agent uses Git/GitHub CLI to create ticket-specific branches, push commits, and open pull requests.

GitHub Actions then provides the controlled Terraform validation and deployment lifecycle.
