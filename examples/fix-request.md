# Example Fix Request

The Jira Function recognizes the `[FIX]` prefix and passes `action = "FIX"` into the ACA Job.

The current agent runtime does not yet implement the incident/fix agent. It reports this path as not implemented.

This example is intentionally documented as a future extension rather than a completed feature.
